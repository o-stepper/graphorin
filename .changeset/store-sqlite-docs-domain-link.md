---
'@graphorin/store-sqlite': patch
---

README concurrency-matrix link now points at `docs.graphorin.com` (eleventh deep retest P2). The previous link put the `/guide/storage` path on the bare landing domain, which serves no docs paths - a live 404 - and the new repo-wide `check-doc-links` gate keeps the whole class out.
