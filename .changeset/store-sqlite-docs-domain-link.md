---
'@graphorin/store-sqlite': patch
---

README concurrency-matrix link now points at `docs.graphorin.com` (eleventh deep retest P2). The previous `graphorin.com/guide/storage` URL was a live 404 - the landing domain serves no docs paths - and the new repo-wide `check-doc-links` gate keeps the whole class out.
