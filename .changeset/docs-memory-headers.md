---
'@graphorin/memory': patch
---

MST-13: the package headers tell the truth — seven tiers (the read-only
insight tier included) and eleven tools plus the gated `deep_recall`
twelfth, replacing the stale "six-tier" / "nine tools" claims. The
`*ForTesting` helpers re-exported from the barrel are now uniformly
tagged `@internal`.
