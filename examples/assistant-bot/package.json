{
  "name": "@graphorin/example-assistant-bot",
  "version": "0.13.0",
  "private": true,
  "description": "The official whole-bot recipe for the Graphorin framework. One long-living personal assistant composed from every framework leg: createAgent with typed tools (one needsApproval HITL gate), createMemory over createSqliteStore with facts auto-recall, SessionManager persistence with JSONL export, createServer REST surface driving run -> awaiting_approval -> resume, a createHeartbeat proactivity beat with a notify outcome, and the channels front door (pairing + sanitization + identity routing) on the loopback adapter. Fully deterministic: an in-tree stub Provider, no network. Created and maintained by Oleksiy Stepurenko.",
  "license": "MIT",
  "author": "Oleksiy Stepurenko",
  "homepage": "https://github.com/o-stepper/graphorin/tree/main/examples/assistant-bot",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/o-stepper/graphorin.git",
    "directory": "examples/assistant-bot"
  },
  "bugs": {
    "url": "https://github.com/o-stepper/graphorin/issues"
  },
  "keywords": [
    "graphorin",
    "ai",
    "agents",
    "framework",
    "example",
    "oleksiy-stepurenko"
  ],
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "tsx ./src/main.ts",
    "build": "tsdown",
    "start": "node ./dist/main.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "smoke": "vitest run",
    "lint": "biome check .",
    "clean": "rimraf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@graphorin/core": "workspace:*",
    "@graphorin/agent": "workspace:*",
    "@graphorin/channels": "workspace:*",
    "@graphorin/memory": "workspace:*",
    "@graphorin/proactive": "workspace:*",
    "@graphorin/security": "workspace:*",
    "@graphorin/server": "workspace:*",
    "@graphorin/sessions": "workspace:*",
    "@graphorin/store-sqlite": "workspace:*",
    "@graphorin/tools": "workspace:*",
    "@graphorin/triggers": "workspace:*",
    "@graphorin/example-trace-helper": "workspace:*",
    "zod": "^3.25.0"
  },
  "bin": {
    "graphorin-example-assistant-bot": "./dist/main.js"
  }
}
